{{- if .Values.restore.enabled }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: sa-mysql-restore
  annotations:
    eks.amazonaws.com/role-arn : {{ .Values.restore.annotations }}
{{- end }}
